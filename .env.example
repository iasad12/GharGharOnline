# Optional TURN fallback. Direct host/STUN P2P remains preferred automatically.
# For Metered's Free plan, standard.relay.metered.ca is used by default.
# VITE_TURN_URLS is optional unless your provider uses a different endpoint.
VITE_TURN_USERNAME=
VITE_TURN_CREDENTIAL=
